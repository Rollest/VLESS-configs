[custom]
# используем свой базовый yaml и ПОЛНОСТЬЮ заменяем правила
enable_rule_generator = true
overwrite_original_rules = true
emoji = false
udp = true

# твой базовый скелет
clash_rule_base = https://raw.githubusercontent.com/Rollest/VLESS-configs/main/base.yaml

# одна группа, в которую попадут ВСЕ узлы + DIRECT
# .* — это regex, чтобы не зависеть от имени ноды
custom_proxy_group = 🚀 Proxy`select`[]DIRECT`.*

# ровно три правила: Chrome и Discord через прокси, всё остальное — мимо
ruleset = 🚀 Proxy,[]PROCESS-NAME,chrome.exe
ruleset = 🚀 Proxy,[]PROCESS-NAME,discord.exe
ruleset = DIRECT,[]MATCH
